% getnextl.m: get the next non-empty line from the string buffer.% if the line contains only comments, it is considered empty.% may modify so 'allow comments' is second input parameter.% Written by:  Jonathan Jacobs%              July 1997  (last mod:  07/04/97)function [nextLine, remaining, isEmptyLine, isEOF] = getnextl(in)[nextLine, remaining] = strtok(in, [13 10]);%if ( isempty(nextLine) | all(isspace(nextLine)) )if isempty(nextLine)   isEOF = 1;   isEmptyLine = 1;   return else   isEOF = 0;endnextLine = stripcom(nextLine);%nextLine = deblank(nextLine);if ( isempty(nextLine) | all(isspace(nextLine)) )   isEmptyLine = 1; else   isEmptyLine = 0;end