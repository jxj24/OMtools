% PWsimp.m: Called by 'PG(simp)' to determine the pulse width
% This is a simple example of how to use an m-file from within SIMULINK
  
% Written by:  Jonathan Jacobs
%              October 1997 

function y = PWfunc( u )

if abs(u) >= 1
   y = (1.3*abs(u) + 12)/1000;
 else
   y = (12*abs(u))/1000;
end